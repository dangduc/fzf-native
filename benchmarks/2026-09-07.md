# fzf-native benchmark report — 2026-09-07

Lower times are better unless a table states otherwise. All live runs used one Apple M3 MacBook Air and ran serially.

## Final main integration

Main advanced to `4c6d810` during this work. That revision added strict fzf normalization parity.

Commit `37d4235` merged the speedups with that revision. It also resolved their overlapping UTF-8 paths.

The final integration used a generated page guard before normalization-table searches.

The table audit derives this guard from the pinned fzf source. It rejects stale page metadata.

### Integrated core probe

Five process pairs ran serially. Odd pairs ran the base first, and even pairs ran the candidate first.

| Case | Base median | Candidate median | Paired speedup | Candidate wins |
|---|---:|---:|---:|---:|
| Chromium shape | 5.140 ms | 2.274 ms | 2.260x | 5/5 |
| Arabic shape | 37.972 ms | 7.695 ms | 4.935x | 5/5 |
| Korean shape | 40.206 ms | 8.939 ms | 4.498x | 5/5 |
| Three-case sum | 83.434 ms | 18.926 ms | 4.394x | 5/5 |

All processes produced the same checksum for each case.

### Integrated session-growth probe

Five process pairs used the same one-million-candidate configuration as the main session test.

| Statistic | Base median process | Candidate median process | Paired speedup |
|---|---:|---:|---:|
| Growth total | 94.956 ms | 17.146 ms | 5.498x |
| Per-round median | 2.329 ms | 0.340 ms | 6.870x |
| Per-round p95 | 2.967 ms | 0.429 ms | 6.916x |
| Per-round maximum | 3.103 ms | 3.434 ms | 0.905x |

All 410 snapshots matched full rescans. Every candidate process stayed at or below depth 32.

The candidate won every total, median, and p95 pair. It won one of five maximum-latency pairs.

The rare full flatten caused the maximum-latency tradeoff. It remained above the measured p95.

### Integrated correctness gates

- The C and ASan/UBSan suites passed.
- All 168 direct Emacs 30.2 tests passed with the rebuilt module.
- The pinned fzf differential passed 5,000 structured cases and 20 session cases.
- The normalization audit matched all 556 entries and all nine source pages.
- ASan/UBSan replay passed nine core, 14 session-state, and 14 reader seeds.
- TSan replay passed all 28 session seeds.

## Pre-merge current-main speedup validation

The original speedup base is `cc18a73`. It was the current `fastducduc/fzf-native` main revision.

The candidate product source is `61ba7a6`. The benchmark telemetry commit is `7e7826d`.

The Rust adapters validated these compiled-input build IDs:

- Base: `84a5368516d01e494d883ddf434461514bac6f73`
- Candidate: `c6318d7923fdffa4b990f117974417fe228e0e70`

All timed benchmark processes ran serially. The process order alternated between base-first and candidate-first pairs.

### Outcome

- The candidate won all 70 paired external holdout comparisons.
- Every paired bootstrap interval remained above 1.0.
- External median speedup ranged from 1.104x to 2.428x.
- Persistent session growth used 82.1% less total time.
- Session median and p95 latency improved by 6.76x and 6.72x.
- The bounded cache policy kept membership depth at or below 32.
- The combined API improved matching cases by 1.75–1.93x.
- Two very short combined-API misses slowed by 13.4–18.4%.

### External holdouts

Each case used ten paired Criterion processes. Five pairs ran the base first, and five pairs ran the candidate first.

Each process used one second of warm-up, a nominal two-second target, and 20 samples.

Speedup is the median of ten paired base/candidate ratios. The interval uses 100,000 deterministic bootstrap samples.

| Case | Base median | Candidate median | Paired speedup | 95% interval | Candidate wins |
|---|---:|---:|---:|---:|---:|
| `emacs` score | 11.296 ms | 8.369 ms | 1.358x | 1.277–1.416 | 10/10 |
| `emacs` score plus positions | 14.621 ms | 11.557 ms | 1.265x | 1.235–1.337 | 10/10 |
| `e-macs` score | 15.163 ms | 13.171 ms | 1.133x | 1.106–1.154 | 10/10 |
| `e-macs` score plus positions | 34.118 ms | 31.259 ms | 1.104x | 1.072–1.142 | 10/10 |
| Chromium | 276.216 ms | 230.553 ms | 1.199x | 1.163–1.279 | 10/10 |
| Arabic | 160.460 ms | 71.517 ms | 2.287x | 2.182–2.384 | 10/10 |
| Korean | 131.148 ms | 52.467 ms | 2.428x | 2.341–2.559 | 10/10 |

The fuzzy score-plus-positions cases call score first. They call positions only after a match.

Thus, those rows do not use the new combined C API.

The Frizbee rows include score time, result collection, copy time, and stable sort time. Frizbee has no positions API.

### Persistent session growth

Each process started with one million candidates. It appended 1,000 candidates in each of 40 rounds.

The test used eight workers, a 10,000-result limit, and query `t`. Untimed full scans validated all 410 snapshots.

| Variant | Operations | Total | Median | p95 | Maximum |
|---|---:|---:|---:|---:|---:|
| Base | 200 | 503.378 ms | 2.455 ms | 3.139 ms | 4.260 ms |
| Candidate | 200 | 90.020 ms | 0.363 ms | 0.467 ms | 3.651 ms |
| Base/candidate ratio | — | 5.592x | 6.763x | 6.722x | 1.167x |

Each candidate process recorded four suffix compactions and one full flatten. No process exceeded membership depth 32.

The full flatten remains the largest candidate latency. The bounded policy reduces its frequency but does not remove it.

This probe includes appends, coordination, worker scoring, cache updates, and result publication.

It does not include a subprocess, pipe decoding, Lisp polling, or Emacs display work.

### Core matcher probe

This synthetic probe isolates the scorer. It is not a replacement for the external holdouts.

| Case | Base median | Candidate median | Paired speedup | Candidate wins |
|---|---:|---:|---:|---:|
| Chromium shape | 3.118 ms | 2.300 ms | 1.400x | 5/5 |
| Arabic shape | 21.246 ms | 7.111 ms | 3.095x | 5/5 |
| Korean shape | 24.485 ms | 8.217 ms | 2.896x | 5/5 |
| Three-case sum | 48.857 ms | 17.682 ms | 2.811x | 5/5 |

All ten processes produced identical checksums for each case.

### Combined score and positions API

The candidate microbenchmark compared the legacy two-call path with the new one-pass API.

Each row used 50,000 calls per sample and 21 alternating-order samples. Equality validation ran before measurement.

| Case | Legacy | One pass | Speedup | Time saved |
|---|---:|---:|---:|---:|
| `emacs` match | 1581.3 ns | 825.8 ns | 1.91x | 47.8% |
| `emacs` miss | 27.4 ns | 32.4 ns | 0.84x | -18.4% |
| `e-macs` match | 1358.1 ns | 777.6 ns | 1.75x | 42.7% |
| Simple miss | 36.8 ns | 41.8 ns | 0.88x | -13.4% |
| Long sparse match | 2088.8 ns | 1151.4 ns | 1.81x | 44.9% |
| Extended query | 1734.5 ns | 1736.9 ns | 1.00x | -0.1% |
| Late AND miss | 316.8 ns | 321.6 ns | 0.98x | -1.5% |
| CJK match | 1370.6 ns | 717.6 ns | 1.91x | 47.6% |
| Case-fold match | 1980.3 ns | 1028.0 ns | 1.93x | 48.1% |

The Emacs module keeps the legacy score-first path. Therefore, the two short-miss costs are not end-to-end regressions.

### Correctness and compatibility evidence

- The C and ASan/UBSan suites passed.
- Parser and scorer failure injection covered 25 and 56 allocation sites.
- All 168 Emacs 30.2 tests passed.
- The pinned fzf comparison passed 5,000 structured cases and 20 session cases.
- An independent review passed 20,000 upstream cases for each scoring scheme.
- A base/candidate differential passed 5.4 million configurations.
- Combined-API sanitizer fuzzing passed 122,601 arbitrary inputs.
- Session ASan and TSan harnesses covered growth, refinement, backspace, limits, stop, destroy, and allocation failure.
- The public C ABI is additive. Existing structure layouts and enum values did not change.
- A client built with the old header linked and ran with the candidate library.

### Retained artifacts

The final run is in `/private/tmp/fzf-native-speedup-holdout-current-main-20260907`.

That directory contains raw logs, Criterion state, exact commands, process order, binary hashes, and derived tables.

The run produced 40 successful Criterion processes, 140 fresh estimates, and 1,120 Criterion JSON files.

## Earlier baseline survey

The remaining sections describe the earlier benchmark survey. They use fzf-native revision `ae3e3e1e8d737949455210f9304dc726ba0e51a1`.

### Main results

- Eight workers reduced the interactive prefix-session median from 485.84 ms to 122.30 ms. This result is a 3.97x speedup.
- Cached backspace results returned in 4.21–13.85 µs. These times are close to the polling floor of the driver.
- On the 95,655-item `emacs` workload, fzf-native took 6.918 ms for scores and 8.853 ms with positions.
- On the harder `e-macs` workload, fzf-native took 10.036 ms for scores and 23.667 ms with positions.
- Frizbee was fastest in all three real data-set groups.
- fzf-native beat Nucleo on Arabic by 1.27x and Korean by 1.51x. Nucleo beat fzf-native on Chromium by 1.72x.
- In Emacs, the fastest fzf-native configuration had a 0.2198 s median for 20 calls. `basic` had a 0.1444 s median.

The clearest optimization target is position retrieval. It added 28.0% on `emacs` and 135.8% on `e-macs` in fuzzy-benches.

### Host and source identity

| Item | Value |
|---|---|
| Host | MacBook Air `Mac15,12`, Apple M3, 8 cores, 16 GB RAM |
| Operating system | macOS 26.5.2, build 25F84 |
| Power check after runs | AC power, battery charging |
| Emacs | GNU Emacs 30.2, development revision `636f166cfc86` |
| C compiler | Apple clang 21.0.0, arm64 |
| Rust compiler | rustc 1.98.1, LLVM 22.1.8, arm64 |
| fzf-native revision | `ae3e3e1e8d737949455210f9304dc726ba0e51a1` |
| Skim fork revision | `5a38f44ad74006a26065a8e1dbedc457672985d3` |
| fuzzy-benches fork revision | `c86546a27974ec00b4d36354738e6f40e464bf65` |
| Frizbee fork revision | `855f03d1b324a692c36ee3c51b20424bc0911c01` |
| Emacs benchmark base | `7f80ffb922581f412fe364566470b5d43d7b7afb` plus the existing local fzf-native integration |

The three benchmark forks and the isolated fzf-native checkout were clean after the runs.

The Emacs benchmark base had the pre-existing local integration changes in `.gitignore`, `bench.el`, and `runbench`.

The Skim driver reported build ID `f49890102ef3eb4ef2d83b341f64f93e9df0c430`.

The Rust adapters reported build ID `728b05dc9e76e91aa920d2965b66599698879f4b`.

Each harness reported a different build ID. Both builds used the same fzf-native revision.

### Interactive session benchmark

The Skim fork supplied the harness. The timed matcher was fzf-native, not Skim.

The driver loaded 1,000,000 generated candidates. Each round emitted at most 10,000 results.

Each configuration used two warmups and 10 measured sessions.

The harness validated each result against an untimed full scan. All 220 JSONL records with a `verified` field contain `true`.

#### Prefix growth

Brackets contain `[p25, p75]`. The speedup is the one-worker median divided by the eight-worker median.

| Query | 1 worker ms | 8 workers ms | Speedup |
|---|---:|---:|---:|
| `t` | 93.04 `[92.76, 93.23]` | 34.15 `[33.42, 34.51]` | 2.72x |
| `te` | 149.00 `[148.22, 149.37]` | 34.34 `[34.09, 34.57]` | 4.34x |
| `tes` | 139.64 `[139.36, 140.20]` | 30.70 `[30.19, 31.14]` | 4.55x |
| `test` | 103.96 `[103.91, 104.28]` | 22.92 `[22.54, 23.37]` | 4.54x |
| **Session total** | **485.84 `[484.32, 486.83]`** | **122.30 `[121.01, 123.42]`** | **3.97x** |

#### Edit trace

The trace was `m -> ma -> mal -> ma -> m`. The last two rounds reused authoritative cached result snapshots.

| Query or total | 1 worker | 8 workers | Speedup |
|---|---:|---:|---:|
| `m` growth | 93.30 ms | 34.32 ms | 2.72x |
| `ma` growth | 127.60 ms | 29.62 ms | 4.31x |
| `mal` growth | 74.37 ms | 17.50 ms | 4.25x |
| `ma` revisit | 5.65 µs | 8.63 µs | 0.65x |
| `m` revisit | 13.85 µs | 4.21 µs | 3.29x |
| **Trace total** | **295.07 ms** | **81.17 ms** | **3.64x** |

The revisit interval includes observer wake latency. The driver requests a 1 µs sleep while it waits.

Thus, the revisit worker ratios are not stable scaling measurements. The repeated hashes and result checksums validate cache reuse.

### fuzzy-benches holdout

Criterion ran 128 cases. Each case used a 3 s warmup, a 5 s measurement target, and 100 samples.

The table contains Criterion median point estimates. The two corpus workloads each contain 95,655 candidates.

#### Full-corpus results

| Subject | `emacs` score ms | `emacs` + positions ms | `e-macs` score ms | `e-macs` + positions ms |
|---|---:|---:|---:|---:|
| `norm-fzf-v1` | **3.405** | **3.521** | **1.787** | **2.021** |
| `norm-fzf-v2` | 4.181 | 4.484 | 3.952 | 4.230 |
| `nucleo` | 4.361 | 4.639 | 4.294 | 4.406 |
| `telescope-fzf-native` | 6.863 | 8.753 | 9.672 | 23.253 |
| `fzf-native` | 6.918 | 8.853 | 10.036 | 23.667 |
| `fuzzy-matcher-skim-v2` | 11.597 | 11.902 | 30.962 | 32.578 |
| `fuzzy-matcher-clangd` | 14.575 | 15.701 | 34.138 | 40.012 |
| `sublime-fuzzy` | 129.738 | 129.536 | 78.897 | 77.924 |

The subjects use different APIs. Therefore, a paired mode does not always isolate only the cost of position retrieval.

For example, sublime-fuzzy uses the same code path for both modes. Some Nucleo pairs also use different matching APIs.

#### Six micro-workload geometric mean

This summary gives equal weight to the six medium and long single-candidate workloads. It uses the geometric mean of Criterion medians.

| Subject | Score µs | Score + positions µs |
|---|---:|---:|
| `norm-fzf-v1` | **0.138** | **0.157** |
| `norm-fzf-v2` | 1.067 | 1.125 |
| `nucleo` | 1.744 | 1.667 |
| `telescope-fzf-native` | 2.324 | 4.734 |
| `fzf-native` | 2.333 | 4.732 |
| `fuzzy-matcher-skim-v2` | 4.991 | 5.055 |
| `fuzzy-matcher-clangd` | 8.556 | 9.835 |
| `sublime-fuzzy` | 36.880 | 36.866 |

#### fzf-native position cost

| Workload | Score | Score + positions | Added time | Overhead |
|---|---:|---:|---:|---:|
| `emacs` | 6.918 ms | 8.853 ms | 1.935 ms | 28.0% |
| `e-macs` | 10.036 ms | 23.667 ms | 13.630 ms | 135.8% |
| Micro geometric mean | 2.333 µs | 4.732 µs | 2.399 µs | 102.8% |

Telescope and direct fzf-native track closely. This result locates most of the paired cost in the native position path, not the wrapper.

### Frizbee holdout

These are nine live sequential cases on real data sets. Criterion used a 0.2 s warmup, a 2 s target, and 10 samples.

Criterion gives a 95% confidence interval for the median. The speed column equals `fzf-native time / subject time`.

A speed value greater than 1 means that the subject was faster than fzf-native.

| Data set | Candidates | Subject | Median ms | 95% CI ms | GiB/s | Speed vs fzf-native |
|---|---:|---|---:|---:|---:|---:|
| Chromium | 1,406,940 | Nucleo | 154.504 | `[153.392, 155.969]` | 0.576 | 1.717x |
| Chromium | 1,406,940 | fzf-native | 265.347 | `[263.723, 267.115]` | 0.335 | 1.000x |
| Chromium | 1,406,940 | Frizbee | **69.925** | `[69.531, 70.751]` | 1.272 | **3.795x** |
| Arabic | 285,587 | Nucleo | 210.961 | `[207.680, 212.436]` | 0.054 | 0.785x |
| Arabic | 285,587 | fzf-native | 165.653 | `[165.200, 166.506]` | 0.069 | 1.000x |
| Arabic | 285,587 | Frizbee | **9.186** | `[9.115, 9.510]` | 1.250 | **18.034x** |
| Korean | 281,471 | Nucleo | 198.677 | `[197.475, 201.615]` | 0.053 | 0.661x |
| Korean | 281,471 | fzf-native | 131.302 | `[130.501, 131.585]` | 0.080 | 1.000x |
| Korean | 281,471 | Frizbee | **8.326** | `[8.235, 8.570]` | 1.258 | **15.771x** |

The `FZF` rows in Frizbee use fixed sleep times from published results. They did not run and do not appear in this live table.

The optional Frizbee debug-strip step did not find `libLLVM.dylib`. Cargo still built and ran the optimized benchmark.

### Emacs completion macrobenchmark

The harness ran in five independent Emacs processes. Each process ran five sweeps of four needles, or 20 completion calls per style.

The table shows the process median and `[p25, p75]`. The fastest fzf-native row is the ratio baseline.

| Style | Median `[p25, p75]` s | vs fastest fzf-native |
|---|---:|---:|
| `basic` | **0.144 `[0.141, 0.145]`** | 0.66x |
| `substring` | 1.474 `[1.461, 1.512]` | 6.71x |
| `hotfuzz` | 0.274 `[0.269, 0.310]` | 1.25x |
| `flex` | 4.718 `[4.656, 5.086]` | 21.46x |
| `fussy` + `flx-score` | 2.615 `[2.549, 2.627]` | 11.90x |
| `fussy` + `flx-rs-score` | 9.823 `[9.749, 10.083]` | 44.69x |
| `fussy` + `fussy-fuz-bin-score` | 1.057 `[1.029, 1.072]` | 4.81x |
| `fussy` + fzf + scoring filter | 0.279 `[0.276, 0.281]` | 1.27x |
| `fussy` + fzf + default filter | 0.238 `[0.218, 0.241]` | 1.08x |
| filter-only fzf + scoring filter | 0.245 `[0.240, 0.261]` | 1.12x |
| **filter-only fzf + default filter** | **0.220 `[0.205, 0.245]`** | **1.00x** |
| `orderless` | 0.655 `[0.653, 0.683]` | 2.98x |
| `orderless` + `flex` | 1.879 `[1.830, 1.892]` | 8.55x |

The fastest fzf-native row was 1.52x slower than `basic`. Its central spread was 18.2% of its median.

The fixed style order and five processes limit inference from close rankings. Process noise changed the fastest fzf-native row in two runs.

The harness disabled the fussy cache and used case-insensitive matching. The exact mean candidate length is 37.579 characters, and the median is 32.

The harness truncates the mean to 37 and labels it as a median. Both operations make the displayed label incorrect.

Emacs warned that it used older byte-compiled files for fussy, flx, and orderless. The same isolated package directory supplied all five processes.

### Interpretation

The interactive session work scales well to eight workers. Cached backspace results avoid a full rescan.

The batch holdouts show a mixed result. fzf-native performs well on Unicode data but loses on Chromium and to the fastest specialized matcher.

The strongest direct target is the position path. The `e-macs` position cost is too large to explain as wrapper overhead.

The Emacs macrobenchmark gives useful end-to-end data. Its process spread and fixed order make it unsuitable for small regression claims.

The host was not in a dedicated benchmark mode. macOS background work and heterogeneous core placement can affect the measurements.

### Data retention

This report contains the derived tables and source identities. The local run also retained raw Criterion samples, JSONL records, logs, and checksums.

The raw output contains 1,124 transient files and is not part of this report commit.
