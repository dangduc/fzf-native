# fzf-native benchmark follow-up — 2026-09-08T04:59:56Z

This report follows [the 2026-09-07 benchmark report](./2026-09-07.md).

It records targeted measurements after the three-round review of pull request 50. Lower times are better.

The formal external holdouts did not run again. Their earlier results remain in the first report.

## Source identity

| Item | Revision |
|---|---|
| Upstream comparison base | `b21edb6d2dbc38e3ca61f19cb2fac3c10a8c5eb2` |
| Original pull request head | `ad7c50a848c5dac733304c8c8f8d8c16c22e44e5` |
| Final product source | `15a86093274e9343055f772452608c180b6a8605` |
| Benchmark-oracle change | `29312a1e04f4b74032583d76f94a9a668b438785` |
| Delivered pull request head | `95dc103a149f6e84cd7c94c18448b96838818e1a` |

Commit `29312a1` changes only the core benchmark oracle. Commit `95dc103` supplies modules built from the final product source.

## Fresh core comparison

Both binaries used the checked-in probe from `29312a1`. The base binary linked the matcher from `b21edb6`.

The candidate binary linked the matcher from `15a8609`. Apple Clang 21.0.0 built both binaries with `-O3 -DNDEBUG`.

Nine process pairs ran serially on the Apple M3 host. Odd pairs ran the base first, and even pairs ran the candidate first.

Each process reported the median of 11 internal samples. The paired speedup is the median of nine base-to-candidate ratios.

| Case | Base median | Candidate median | Paired speedup | Candidate wins |
|---|---:|---:|---:|---:|
| Early ASCII hit | 0.799 ms | 0.533 ms | 1.503x | 9/9 |
| One-byte candidates | 0.280 ms | 0.261 ms | 1.065x | 9/9 |
| Chromium shape | 3.750 ms | 1.688 ms | 2.217x | 9/9 |
| Arabic shape | 27.287 ms | 5.404 ms | 5.057x | 9/9 |
| Korean shape | 28.962 ms | 6.172 ms | 4.692x | 9/9 |
| UTF-8 late hit | 28.336 ms | 14.474 ms | 1.958x | 9/9 |
| UTF-8 v1 early hit | 41.076 ms | 31.013 ms | 1.329x | 9/9 |
| UTF-8 v1 late hit | 89.574 ms | 47.488 ms | 1.886x | 9/9 |

The candidate won all 72 paired case comparisons. The paired speedup range was 1.065x to 5.057x.

Every process produced the same case checksums. The exact-score and membership fingerprints also matched before each timed run.

This probe isolates scorer costs. It does not replace the external fuzzy-benches and Frizbee holdouts.

## One-byte review probe

This probe compares upstream main, the original pull request, and the final product source. It calls public `fzf_get_score`.

Each value is the median of nine process p50 values. Each process used 101 samples of 20,800 calls.

| Case | Main | Original pull request | Final | Final/main | Final/original |
|---|---:|---:|---:|---:|---:|
| All hit | 33.99 ns | 16.97 ns | 14.62 ns | 2.32x | 1.16x |
| Folded hit | 25.14 ns | 20.48 ns | 16.44 ns | 1.53x | 1.25x |
| All miss | 8.08 ns | 10.96 ns | 7.93 ns | 1.02x | 1.38x |
| One hit in 26 | 8.89 ns | 12.31 ns | 8.46 ns | 1.05x | 1.46x |

All nine processes produced stable labels and checksums for each revision.

A direct internal case-sensitive miss increased from 2.84 ns to 3.17 ns. The public all-miss path remained 1.02x faster than main.

## Session stability after the final specialization

Fifteen alternating process pairs compared `f691c95` with `15a8609`. This comparison isolates the final one-byte specialization.

Each process started with 50,000 candidates. It added 1,000 candidates in each of 40 rounds.

The probe used four workers, an unlimited result set, and query `t`.

| Statistic | `f691c95` median | `15a8609` median | Paired ratio | Final wins |
|---|---:|---:|---:|---:|
| Initial request | 3.702 ms | 3.697 ms | 1.003x | 8/15 |
| Growth total | 34.548 ms | 34.163 ms | 1.020x | 11/15 |
| Per-round median | 0.843 ms | 0.819 ms | 1.010x | 9/15 |
| Per-round p95 | 1.189 ms | 1.141 ms | 1.053x | 9/15 |
| Per-round maximum | 1.388 ms | 1.352 ms | 1.049x | 9/15 |

The median resident set size changed from 109.14 MiB to 108.27 MiB. All 1,230 full-scan snapshot comparisons passed.

The small timing differences are not evidence of a session speedup. They show no visible regression from the final specialization.

## Benchmark and correctness gates

- The independent session oracle uses libc `qsort` and compares the full result order.
- A deliberate correlated-sort mutation now fails the session benchmark.
- The core probe uses pinned, two-lane fingerprints for exact scores and membership.
- A deliberate score mutation now fails before the core probe starts its timer.
- Exhaustive one-byte testing covered 1,048,576 byte, mode, and direction combinations.
- The C, ASan/UBSan, TSan, and differential suites passed.
- All 188 direct Emacs 30.2 tests passed with a fresh module.
- The committed arm64 module passed all 40 artifact smoke tests.
- All applicable pull request checks passed at `95dc103`.

## Scope

This follow-up does not contain new fuzzy-benches, Frizbee, one-million-candidate, combined-API, or Emacs macrobenchmark results.

Those earlier holdouts compared `61ba7a6` with `cc18a73`. The first report gives their method, measurements, and retained artifacts.

The new data targets the paths changed during review. It supports the final pull request without relabeling the earlier holdout data.

## Reproduction and retained data

The repository supplies these benchmark commands:

```sh
make benchmark-core-hotpath-probe
make benchmark-session-growth
make benchmark-score-positions
```

These commands exercise the two benchmark oracles:

```sh
make ctest-core-hotpath-oracle
make ctest-session-growth-benchmark
```

The fresh nine-pair core logs are in `/private/tmp/fzf-pr50-followup.oZp0qr` on the measurement host.

The review telemetry and mutation evidence are in `/private/tmp/fzf-pr50-evidence` on the same host.
